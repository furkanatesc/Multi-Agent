"""Test Generator agent package (Sprint 5).

Exposes the :class:`TestGeneratorAgent`, which produces unit/widget/integration
tests for generated source and (when Docker is available) measures coverage
against the ≥70% target.
"""
